@page "/"
@inject ViewerInfo gameInfo
@inject ILogger<Index> logger

<PageTitle>HungryGame REST Viewer</PageTitle>
<Board />

@code
{
    protected override void OnInitialized()
    {
        gameInfo.UpdateTick += (_, _) => InvokeAsync(() => StateHasChanged());
    }
}